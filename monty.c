BRAINSTORMING

if (argv < 1 || argv == NULL)
{
	fprintf("USAGE: monty ./monty.c\n");
	exit(EXIT_FAILURE);
}
read(monty.c, RONLY|truncate);
fprintf("Error: Can't open file $file_name\n");
exit(EXIT_FAILURE);
if ($file_name != valid_instruc)
{
	fprintf("L<line_number>: unknown instruction <opcode>\n");
	exit(EXIT_FAILURE);
	line_number = 1;
	line_number = argc;

	if (!malloc)
	{
	fprintf("Error: malloc failed\n");
	exit(EXIT_FAILURE);
